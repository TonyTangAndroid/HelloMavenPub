package com.example.hello_jar;


public class HelloJarUtil {

    /**
     * @param raw the raw string to be duplicated.
     * @return a duplicated string.
     */
    public static String duplicate(String raw) {
        return raw + " Jar";
    }
}